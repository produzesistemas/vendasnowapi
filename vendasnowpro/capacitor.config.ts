import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vendasnowpro_ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
